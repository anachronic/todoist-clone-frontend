query tasks($done: Boolean = null, $projectId: String, $forToday: Boolean) {
  tasks(filters: { done: $done, projectId: $projectId, forToday: $forToday }) {
    id
    text
    done
    schedule
  }
}
