mutation updateProject($id: String!, $name: String, $colorId: Float) {
  updateProject(project: { id: $id, name: $name, colorId: $colorId }) {
    id
    name
    color {
      id
      hex
    }
  }
}
