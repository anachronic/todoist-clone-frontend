mutation updateProject($id: String!, $name: String, $colorId: String) {
  updateProject(project: { id: $id, name: $name, colorId: $colorId }) {
    id
    name
    color {
      id
      hex
    }
  }
}
